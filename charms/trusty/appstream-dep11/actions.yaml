mirror-mounted:
    description: Try to mount the mirror device
    params:
        device:
            type: string
            description: The device the mirror is available on
    required: [device]
upgrade:
    description: Update the code from upstream
